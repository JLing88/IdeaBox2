<h1>All Ideas for <%= @user.username %></h1>

<%= link_to "Create an Idea", new_user_idea_path(@user)%>

<% @ideas.each do |idea| %>
<ul>
  <h3><%= link_to idea.title, user_idea_path(@user, idea) %></h3>
  <p><%= idea.body %></p>
</ul>
<% end %>
